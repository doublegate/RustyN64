# Phase 6 — Frontend Integration

**Goal:** wire the egui shell to the real VI scanout + AI audio drain + the SI
joybus controller input; wasm parity + save-states / rewind / run-ahead in the
frontend (the determinism contract: rate control lives here, never in the core)

**Exit criteria:** see the skill's references/roadmap_template.md for the per-phase exit bar.

## Sprints

- Sprint 1 — TODO (add `sprint-1-<slug>.md` with `T-61-NNN` tickets: title, description,
  acceptance criterion = the test ROM / pass condition that closes it, dependencies, complexity).
