# Phase 5 — Cart Boot + Saves

**Goal:** the PI cart pipeline plus the PIF/CIC boot handshake (IPL2/IPL3
seeding) and the save backends (EEPROM 4k/16k, SRAM, FlashRAM) round-tripping

**Exit criteria:** see the skill's references/roadmap_template.md for the per-phase exit bar.

## Sprints

- Sprint 1 — TODO (add `sprint-1-<slug>.md` with `T-51-NNN` tickets: title, description,
  acceptance criterion = the test ROM / pass condition that closes it, dependencies, complexity).
