# Phase 3 — RDP LLE + VI

**Goal:** the LLE RDP rasterizer (triangle/rectangle edge-walk, the color
combiner, the blender, framebuffer writes) plus the VI scanout, producing a
stable rendered frame

**Exit criteria:** see the skill's references/roadmap_template.md for the per-phase exit bar.

## Sprints

- Sprint 1 — TODO (add `sprint-1-<slug>.md` with `T-31-NNN` tickets: title, description,
  acceptance criterion = the test ROM / pass condition that closes it, dependencies, complexity).
