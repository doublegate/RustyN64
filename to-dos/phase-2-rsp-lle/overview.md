# Phase 2 — RSP LLE

**Goal:** the LLE RSP (the MIPS+SIMD vector coprocessor that runs game-supplied
microcode) executing under master-clock lockstep against the scheduler

**Exit criteria:** see the skill's references/roadmap_template.md for the per-phase exit bar.

## Sprints

- Sprint 1 — TODO (add `sprint-1-<slug>.md` with `T-21-NNN` tickets: title, description,
  acceptance criterion = the test ROM / pass condition that closes it, dependencies, complexity).
