# Phase 7 — Accuracy Breadth

**Goal:** drive the accuracy battery to target across the game corpus (microcode
variants, save types, region timing NTSC/PAL as data); defer hard residuals to
the future sub-cycle timebase (ADR 0002)

**Exit criteria:** see the skill's references/roadmap_template.md for the per-phase exit bar.

## Sprints

- Sprint 1 — TODO (add `sprint-1-<slug>.md` with `T-71-NNN` tickets: title, description,
  acceptance criterion = the test ROM / pass condition that closes it, dependencies, complexity).
