# Phase 4 — AI Audio

**Goal:** the AI (Audio Interface) DMA-fed by the RSP audio microcode output,
through the band-limited/resampling mixer to the host audio ring

**Exit criteria:** see the skill's references/roadmap_template.md for the per-phase exit bar.

## Sprints

- Sprint 1 — TODO (add `sprint-1-<slug>.md` with `T-41-NNN` tickets: title, description,
  acceptance criterion = the test ROM / pass condition that closes it, dependencies, complexity).
